
export const Levels =()=>{

    // map through an arraylist of all of the levels.
    // each level should have a lock property. Based on that it should be clickable. This property can just be true or false. 
    // and it will check to make sure user is still authenticated. If the user logs out, then 
    return(
        
    <div>
        <h1>Levels</h1>
    </div>


    );
}

